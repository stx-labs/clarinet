# CLAUDE.md

The instructions for this repository live in `AGENTS.md`, imported below so that
Claude Code, Codex and anything else reading either name get one file.

A regular file with an import rather than a symlink: git for Windows leaves
symlinks unmaterialised unless `core.symlinks` is on, which would turn this into
a stub whose entire content is the word `AGENTS.md` — and every rule below would
be silently gone with nothing to notice it by.

@AGENTS.md
