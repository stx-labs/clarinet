{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r \".tool_input.file_path // empty\" | { read -r f; case \"$f\" in *.rs) jq -nc \"{hookSpecificOutput:{hookEventName:\\\"PreToolUse\\\",additionalContext:\\\"This repo has Rust conventions at .agents/skills/rust-conventions/SKILL.md — read it before editing if you have not already this session. Most-missed rules: no println!/eprintln! in wasm-reachable crates (use clarity_repl::uprint!/ueprint!); derive epoch and Clarity-version defaults from clarinet_defaults rather than writing literals; indoc! for multi-line strings; strum derives for enum boilerplate.\\\"}}\" ;; esac; }"
          }
        ]
      }
    ]
  }
}
