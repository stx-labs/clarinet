{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$(jq -r '.tool_input.file_path // empty')\" in *.rs) jq -nc --arg c \"Rust here has wasm32 and reuse constraints that a generic Rust reviewer will miss. Read .agents/skills/rust-conventions/SKILL.md before editing, if you have not already this session.\" '{hookSpecificOutput:{hookEventName:\"PreToolUse\",additionalContext:$c}}' ;; esac"
          }
        ]
      }
    ]
  }
}
